# Data / logic bugs found while simplifying jargon (2026-07-22)

> **CLOSED 2026-07-22.** All three severity tiers have been worked. Outcome:
> HIGH 10 fixed / 1 not fixable here (frozen archive setups is a Pi-side data
> defect). MEDIUM all worked. LOW 11 fixed, 5 judged not-a-bug with the
> reasoning left in code comments, 1 deferred. Entries below are the ORIGINAL
> findings and are kept as the historical record — read the git log for what
> each fix actually did.

Surfaced by tracing every user-visible label back to the value it renders.
NONE of these were fixed — this was a copy change. Each is a real defect in
data or computation, not wording.

Total: 58 (11 high, 31 medium, 16 low)

> **Status update (2026-07-22):** all 11 HIGH-severity items below are now
> **fixed** except the frozen `premarket_top_setups` archive data, which is a
> Pi-side data-generation defect and cannot be fixed from this repo — the
> committed archive files simply contain the same setups array on 43 of 53
> dates. The MEDIUM and LOW items are untouched.
>
> **MEDIUM severity is now also addressed** (see the commit following this file's
> last update). Two Python fixes in `pi-scripts/generate-screener-data.py` change
> future generated data: RSI now uses Wilder's smoothing (the previous simple
> rolling mean disagreed with every charting platform), and the analyst-rating
> check now matches lowercase keys, so the sixth scoring rule finally fires and
> some scores shift by +/-1. `push_dashboard.py` was deliberately NOT changed —
> the deployed Pi copy was already fixed on 2026-07-21 and shipping this repo's
> stale mirror would regress production.
>
> Two fixes changed what the site displays:
> - The options regime chip now derives from **signed** dealer gamma. It had
>   been reading the gross total, in which puts are stored positive, so it was
>   structurally almost always "bullish". On current data it now reads bearish
>   (signed −521,441) instead of bullish (gross +2,671,051).
> - The screener's Strategy and Direction dropdowns were **removed** rather than
>   repaired: they filtered on fields no generator writes, so every selection
>   returned zero rows, and the dropdown options had no principled mapping onto
>   the rule keys the generator does emit.


## HIGH severity

- **src/app/charts/charts-client.tsx:82** _( charts-models-ticker )_
  calcVWAP initialises cumV/cumPV once and never resets, so the line labelled VWAP is a cumulative volume-weighted average over the ENTIRE loaded series (an anchored VWAP from the first bar), not the session VWAP the acronym means. On the Weekly/Monthly/Yearly toggles it is weighting weekly/monthly/yearly bars, which has no relation to the intraday concept at all. Because of this I deliberately did NOT attach the glossary `vwap` tooltip anywhere on the charts page — its wording ('the average price paid so far today') would be false for what is plotted. Fixing the calculation (reset per session) or renaming the series would let the tooltip be wired.

- **data/archive/2026-07-21.json:1** _( dashboard-archive-account )_
  `premarket_top_setups` is frozen. Across the 53 files in data/archive/, 43 carry a byte-identical setups array (MSFT score 11 / ARKK score 10 / IBIT score 10, same prices to 12 decimal places, same rsi, same signal lists), 8 are empty, and 2 use an older shape. Verified by hashing the serialized array for every archive file. Every /archive/[date] page therefore publishes the same 'Premarket Setups' regardless of date, which makes the word 'Premarket' — implying a fresh pre-open scan — unsupported. I removed the 'before the market opened' freshness claim from the explanatory copy I added, but left the section title and the data alone.

- **src/lib/schemas/market.ts:195** _( dashboard-archive-account )_
  `gamma_regime: regimeOf(bucket.total_gex)` derives the regime from the GROSS gamma total, in which puts are not signed negative (the file's own comment at line 234 says total_gex is 'the legacy gross number'). A gross total is essentially always positive, so regimeOf's `totalGex > 0 ? 'bullish'` branch is the only one that can realistically fire — the regime is a constant, not a reading. It also contradicts the signed figure in the same record: total_gex is +2,671,051 while total_dealer_gex is -521,441 (dealers short gamma, i.e. destabilizing). A beginner following the 'Go Deeper' link from an archived briefing lands on /options showing the opposite lean to the regime chip. Signed dealer gamma is presumably what regimeOf should read, but that is a calculation change, so I left it.

- **src/lib/schemas/market.ts:205** _( options )_
  Merged-row transform zeroes the put leg. gamma_profile rows in data/gex-detail.json carry a single combined oi/dex/vex per strike; the transform hands the whole combined value to the CALL row (line 202) and hardcodes 0 into the PUT row whenever calls also exist at that strike. Live effect: strike 745 renders 'Put GEX 241.4K' next to 'OI 0, DEX 0, VEX 0', both in the flow table and in the GammaWallChart hover card, and the same artifact is announced to screen readers. Not fixable in copy — a beginner cannot tell a real zero from a merge artifact.

- **src/lib/schemas/market.ts:195** _( options )_
  The page-header regime chip and its sentence are driven by regimeOf(bucket.total_gex) (helper at line 176), a GROSS sum in which put_gex is stored positive (verified: strike 700 put_gex = +76,581, while docs/options-intelligence/gex_calibration_report.md gives the calibrated put convention as -1.152676). On the current payload that gross figure is +2,671,051 → 'BULLISH', while total_dealer_gex on the SAME payload is -521,441 → 'short'. The page therefore shows a green stability read at the top and a red one directly beneath it, from two different conventions applied to the same option chain. I disclosed the conflict in the header copy and the Dealer Positioning fine print, but the two figures still disagree.

- **src/app/positions/positions-client.tsx:194** _( positions )_
  LIVE double-count of unrealized P&L in the hero 'Equity' figure. Line 194 recomputes equity client-side as starting_balance + total_pnl + unrealized_pnl instead of reading portfolio.total_balance, which the producer already publishes and which is rendered two tiles away as 'Total Value'. Under the live 2026-07-21 Pi producer contract (recorded in project memory: total_pnl = realized + unrealized, total_balance = cash + market_value) this adds unrealized twice, so 'Equity' overstates the account by exactly unrealized_pnl and disagrees with 'Total Value' on the same screen. It also inflates the denominator of the '% invested' figure beside it, understating how much of the account is committed. Not fixed — this is a calculation. Caveat: paper_trades.json is a gated R2 file I could not fetch, so the direction of the error is inferred from the producer contract, not observed. This is why I did NOT attach a plain-English tooltip to 'Equity': asserting 'what the account is worth' would be confidently wrong.

- **src/app/predictions/predictions-client.tsx:254** _( predictions )_
  "Live Sharpe (est)" is not a Sharpe ratio. estSharpe = (bestWR - 50) / 15, a linear rescale of a win rate regex-scraped out of the summary.best_win_rate string — no return series or volatility is involved. The displayed value is then estSharpe * 0.7 (line 255) and is checked against a >= 1 pass threshold, so a fabricated proxy drives one of the seven Research Score points. I rewrote the label and detail copy to say so, but left the maths untouched.

- **src/app/screener/screener-client.tsx:88** _( screener )_
  The Strategy filter is dead. It compares against `t.signal` (singular), a field no producer in the repo ever writes — pi-scripts/generate-screener-data.py emits only `signals` (plural) and `score`, and the field is absent from every row of data/screener-lite.json. src/lib/schemas/screener.ts:30 declares it nullish so the parse succeeds and the value is always undefined. Selecting Momentum, Breakout, Mean Reversion or Support/Resistance returns zero rows in both the public teaser and the paid dataset. This is the single worst trap on the page for a beginner, who will read an empty table as 'no stocks qualify'. Left the label alone because no wording can fix a filter that cannot match.

- **src/app/screener/screener-client.tsx:106** _( screener )_
  The Direction filter is dead in the same way. It filters on `t.direction`, declared at src/lib/schemas/screener.ts:31 but never emitted by the generator and absent from every data row. Selecting Long or Short always returns zero rows.

- **src/app/screener/screener-client.tsx:164** _( screener )_
  BEARISH_SIGNAL = /over|bear|below|low|extended|death|premium|sell/ matches the substring 'over' inside 'oversold_rsi', so the largest BULLISH contributor to the score (+2, RSI < 35) is painted in the bear/red palette. NOTE THE INTERACTION WITH MY CHANGE: the chip now reads the truthful 'RSI under 35' but is still rendered red, which makes the wrong colour more conspicuous than the old 'oversold rsi' text did. I did not touch the regex because colour is derived display logic, not copy. The same regex also mis-colours nothing else I could find, but 'near_low' correctly matching 'low' is coincidental rather than principled.

- **src/lib/schemas/market.ts:176** _( shell-and-primitives )_
  OUTSIDE MY FILES — reported, not touched. `gamma_regime` is derived as `total_gex > 0 ? 'bullish' : total_gex < 0 ? 'bearish' : 'neutral'`, but `total_gex` is documented at market.ts:236-237 as the legacy GROSS total (puts are not signed negative), so it is effectively always positive and the 'bearish' branch is unreachable. Today's data has total_gex = +2,671,051 while the genuinely signed `total_dealer_gex` = −521,441, so RegimeChip renders BULLISH on the options pages while dealers are actually short gamma, and options-client.tsx:94 then prints 'Dealer hedging is stabilizing — dips are likely to be bought.' No amount of relabelling in RegimeChip can fix this; the sign convention has to change at the source.


## MEDIUM severity

- **src/app/ticker/ticker-client.tsx:105** _( charts-models-ticker )_
  score falls back to scan.score (latest.json premarket_top_setups) when council_analysis.score is missing. That fallback is a different, unclamped scale — current live values include 10, 10 and 11, above the 1-10 ceiling compute_score() enforces. Two incompatible scales are rendered under one 'Score' label, which is also why I could not safely caption the card 'out of 10'.

- **src/app/charts/charts-client.tsx:261** _( charts-models-ticker )_
  The RSI guide lines are drawn at 70/30 (now labelled Overbought/Oversold), but the scoring pipeline that produces the ticker page's Score switches at 75/65/45/35 (compute_score() in pi-scripts/generate-screener-data.py). The chart and the score therefore disagree about what counts as an extreme reading. Left alone: reconciling them is a calculation decision, not copy.

- **src/app/archive/[date]/page.tsx:219** _( dashboard-archive-account )_
  The Market Snapshot renders the raw `market_summary.indices` array, which in the data also contains VIX and '10Y Yield' as pseudo-index entries. The Level cell is `Number(x.price).toLocaleString(...)`, so the 10-year yield prints as a bare '4.6' with no percent sign — while the very same field on the /archive index page is printed as '4.6%'. Fixing needs per-row type logic (the array mixes index points, a volatility reading and an interest rate under one shape), which is a data-shape change, so I renamed the column header from 'Index' to 'Market' and wired glossary tooltips onto the VIX and 10Y row labels but left the formatting alone.

- **src/app/archive/[date]/page.tsx:221** _( dashboard-archive-account )_
  For the '10Y Yield' pseudo-row, `change_pct` is the RELATIVE percent change of the yield, not a move in percentage points (2026-07-21 records ten_year_yield 4.6 with change_pct 1.32 — a 1.32% relative move, i.e. about +0.06pp). Rendered as '+1.32%' next to a level of '4.6' it reads as though the yield moved 1.32 points. My 'Change' -> '1-day change' header rename fixes the missing period and unit but cannot fix this: the two rows need different units in the same column. Same value is also the source for the VIX row's change.

- **src/lib/schemas/market.ts:226** _( dashboard-archive-account )_
  The GEX record keyed `tickers.SPX` in data/maplegamma-data.json carries current_price 748.72, gamma_flip 751.26 and max_pain 740.0 — SPY prices, roughly one tenth of the S&P 500 index. GexDataSchema hardcodes `ticker: 'SPY'`, which agrees with the prices but not with the JSON key, and the surrounding copy (page titles, the archive 'Go Deeper' link) all says S&P 500. The levels are correct for the ETF and wrong by a factor of ten for the index, so a beginner comparing gamma_flip 751.26 against an S&P 500 print of 7,443 gets nonsense. Naming/keying issue in the data feed, not something a copy change can resolve.

- **src/components/feature/market/VixRegimeCard.tsx:51** _( market-verdict-news )_
  The VIX delta colour is inverted: `vixChange < 0 ? 'var(--color-bear)' : 'var(--color-bull)'` paints a RISING VIX bull-green and a FALLING VIX bear-red. That is backwards under either convention (a rising VIX means more fear, and a falling VIX is the outcome that is good for shares), and it contradicts the card's own regime logic three lines above, which treats a high VIX as bearish. My copy edits make the contradiction more visible, since the card now says in words that over 25 is stressed while the arrow beside the number still turns green as fear rises. Left untouched because a colour mapping is outside a copy-and-labelling change.

- **src/components/feature/verdict/VerdictBar.tsx:85** _( market-verdict-news )_
  model_features.recent_hit_rate is 0.5 while model_features.recent_trades is 0 in the live verdict.json — with zero recent trades this 50% is a hardcoded prior, not a measured track record, yet it renders in the hero bar with the same authority as real data. Same picture across the block: sleeve_a_signals, avg_signal_score and max_signal_score are all 0. Rendering a caveat would require new conditional logic on the data, so I only relabelled what the number is.

- **src/components/feature/verdict/VerdictBar.tsx:72** _( market-verdict-news )_
  confidence_interval is displayed as a model interval, but the live verdict.json reports model_features.calibration_source = 'heuristic_fallback' — the band is a default heuristic, not derived from backtested calibration. Meanwhile 'Model Calibration' is sold as a Pro feature elsewhere on the site.

- **pi-scripts/push_dashboard.py:553** _( market-verdict-news )_
  The repo's copy still publishes the superseded per-share formula `invested = sum(p['entry_price'])` (a one-share-per-position shadow portfolio). The R2 file the Portfolio card actually fetches comes from the quantity-scaled version fixed on the Pi 2026-07-21, so the repo script is stale relative to production. My 'measured at what they cost to buy' caption is true of the fixed Pi output.

- **src/lib/schemas/market.ts:226** _( options )_
  Ticker is hardcoded to 'SPY' while the source JSON stores everything under tickers.SPX. The values are unambiguously SPY-scale (current_price 748.72, strikes 700–800), so the label is right and the JSON key is wrong, but nothing in the pipeline reconciles them. I corrected the page metadata copy from SPX to SPY; that does not fix the underlying key/value inconsistency, and the hardcoded literal means the chart title and aria-label will keep saying SPY even if the payload ever changes underlying.

- **src/app/options/options-client.tsx:23** _( options )_
  mode.strikes.slice(0, 10) selects rows by array position, and the array is built in ascending strike order, so the table always shows the ten lowest strikes. On the current file that is 700–745 (nine of the ten are puts) and the largest-GEX strike, 750 with net_gex 598,589, is never rendered. The stale comment at GammaWallChart.tsx:19-20 asserts the opposite ('strikes[] arrives sorted by GEX magnitude'), which is presumably where the original 'Top Strikes' title came from. I renamed the header to stop claiming a ranking, but the selection itself is still arbitrary rather than most-significant.

- **src/components/feature/options/GexDexVexCard.tsx:50** _( options )_
  Out of my edit scope (GexDexVexCard was excluded), reported for completeness: SignBar plots GEX, DEX and VEX against a single shared maxAbs. The three are different units of different magnitudes (2,671,051 / 65,602,882 / 991,228,881), so the GEX bar is drawn against a 991M axis and renders visually as zero. Three incompatible units on one scale.

- **src/app/positions/positions-client.tsx:364** _( positions )_
  The entire 'Prediction Engine Accuracy' card is unreachable. It is gated on acc?.overall, but accuracy.json has no 'overall' key: AccuracySchema (src/lib/schemas/market.ts:280-317) documents the real shape as {summary, expectancy, drawdown, ...}, and src/app/predictions/predictions-client.tsx:170-186 reads exactly those keys off the same file. The card is a verbatim port of legacy assets/js/paper-trades.js:465-484 and renders nothing for anyone. Its title also claims backtest-engine accuracy while pointing at a file market.ts:323 explicitly calls 'live-sim accuracy, a different thing'. Left untouched rather than relabelled, since relabelling invisible copy is zero user value. Inferred from schema + producer; the gated file was not fetchable to confirm.

- **src/app/positions/positions-client.tsx:374** _( positions )_
  The 'Best Strategies' card is unreachable for the same reason: it is gated on acc?.top_performers?.length and accuracy.json has no 'top_performers' key (see AccuracySchema, src/lib/schemas/market.ts:280-317). Consequence for this task's numbers: of the 18 tooltips I wired, 5 (backtest, win_rate, avg_pnl, pf, sample_size) and 2 of the renames (Backtest Verified → Backtest Results, Confidence → Evidence) live in this card, so only 13 tooltips are actually user-facing today. I kept those edits because they are correct if the data key ever appears, but they should not be counted as coverage. Inferred from schema; gated file not fetchable to confirm.

- **src/app/positions/positions-client.tsx:350** _( positions )_
  Option premium and P&L appear not to be scaled by the 100-share contract multiplier. Lines 350-351 render pos.entry_price and pos.current_price — the same generic per-unit fields used for stocks — under the headers 'Premium Paid' and 'Current Premium', and line 352 renders pos.pnl the same way. A premium quoted at $2.40 costs $240 for one contract, so a beginner reads the cost of the position as roughly 1/100th of what it is. Conditional: the 2026-07-21 producer fix recorded in project memory covers quantity-scaling for stock positions only and says nothing about option multipliers, and I could not fetch the gated file to check whether option rows carry a quantity or contract-size field. Left alone — fixing it would change a displayed calculation.

- **src/app/predictions/predictions-client.tsx:253** _( predictions )_
  Reward-to-risk is never measured. estRR = (bestPF * (1 - winRate)) / winRate back-solves it algebraically from the profit factor and win rate, both regex-scraped out of pre-formatted strings. It is presented as a measured trade statistic with an "R" suffix and gates a Research Score check.

- **src/app/predictions/predictions-client.tsx:259** _( predictions )_
  Sample Size renders `${(totalTrades / 1000).toFixed(0)}K trades`, so any total under 500 displays as "0K trades" — including totals that pass the >= 100 check, producing a green tick next to "0K trades".

- **src/app/predictions/predictions-client.tsx:231** _( predictions )_
  Validation reads walk_forward_v2.json via a top-level `windows` array, while WalkForwardTile.tsx (line 51) reads the same file via a `summary` record keyed by strategy. The two components disagree on the file's shape, so one of them can silently fall back to its empty state ("Not yet run") while the other renders data.

- **src/components/feature/prediction/CalibrationChart.tsx:49** _( predictions )_
  The tile's stated purpose cannot be computed from the data it fetches. Neither accuracy.json nor AccuracySchema carries any per-trade predicted probability; the only value consumed is data.total_signals, a raw count of closed trades. The old copy asserted trades had "been matched to predicted probabilities" — I rewrote the copy to stop claiming that, but the underlying data gap means the promised curve cannot be produced at 30 trades or at any other count without a schema change.

- **src/app/predictions/predictions-client.tsx:184** _( predictions )_
  Max Drawdown in Live Simulation Metrics is computed by the Pi generator from a synthetic equity curve that starts at 100.0 and compounds each closed trade's percent return sequentially, as if 100% of capital were in every trade one at a time. It is not the drawdown of the $100K paper account and ignores position sizing, yet it sits in a card headed "Live Simulation" beside the real simulated account elsewhere on /models. I added a footnote stating this; the calculation is unchanged.

- **src/app/research/research-client.tsx:929** _( research )_
  The r/stocks Bullish/Bearish badge is effectively hard-wired to 'Bullish'. The test is `String(src.sentiment_summary).includes('BEARISH')`, but reddit-sentiment.json's `stocks` branch emits a different vocabulary — 'CONSTRUCTIVE' / 'CRITICAL' — and never contains the literal 'BEARISH' (verified against the live public/data/reddit-sentiment.json, which reads '🟢 **CONSTRUCTIVE** — Community focused on opportunities and analysis'). The wsb branch does emit BULLISH/BEARISH, so only that card works. Left alone: fixing it means changing the comparison logic, not the copy.

- **src/app/research/research-client.tsx:1328** _( research )_
  The Degradation column colours values green when `(s.avg_degradation_pct ?? 0) >= 0` and red only when negative — i.e. the more of the edge that disappeared out-of-sample, the healthier it looks. Higher degradation is worse; the sibling component src/components/feature/prediction/WalkForwardTile.tsx reddens values above 50%. My new footnote under the table now tells the reader that bigger is worse, which directly contradicts the colour until the conditional is fixed.

- **src/app/research/research-client.tsx:1278** _( research )_
  The Sharpe figures 2.22 and 1.55 in the Backtest explainer are hardcoded JSX literals — they are not read from walk_forward_v2.json and never change with the table underneath them. I reworded the sentence to present them explicitly as an illustration rather than today's numbers, but the underlying copy is still a static claim sitting above live data.

- **src/app/screener/screener-client.tsx:308** _( screener )_
  The RSI cell colouring contradicts the score. RSI below 30 is painted bear-red while compute_score() treats RSI below 35 as its strongest bullish input (+2), and RSI above 70 is painted bull-green while the score penalises anything above 65 (-1) and above 75 (-2). The table therefore colours the same reading in opposite directions in two adjacent columns.

- **src/app/screener/screener-client.tsx:299** _( screener )_
  Market cap is always divided by 1e9 and suffixed 'B', so a $4T company renders as '4000.0B'. A user who selects the 'Over 1 trillion' bucket sees no trillion-scale formatting anywhere in the results. No currency symbol is shown either, and TSX (.TO) rows carry CAD market caps in the same column as USD rows.

- **src/app/screener/screener-client.tsx:90** _( screener )_
  A missing marketCap is coerced to 0 before bucketing, so rows with no market-cap data silently land in the smallest bucket (now labelled 'Under 2 billion') rather than being excluded or shown separately.

- **src/app/screener/screener-client.tsx:503** _( screener )_
  The 'Avg Score' StatCard reads market_summary.avg_score, which is precomputed on the Pi across the entire 659-row universe. It does not respond to any filter the user sets, and in the public 8-row preview it describes 651 rows the visitor cannot see. The card carries no denominator or 'all tickers' qualifier. The same is true of Closed Up / Closed Down, and green_count + red_count need not sum to Scanned because flat names are counted in neither (280 + 377 = 657 of 659 in the current file).

- **pi-scripts/generate-screener-data.py:325** _( screener )_
  The analyst-recommendation term of compute_score() is dead code: it tests `rec in ('BUY','STRONG_BUY')` while yfinance returns lowercase recommendationKey values ('buy', 'strong_buy'). Confirmed in data/screener-lite.json, where a row with recommendation:'buy' carries no analyst_buy signal. The analyst input the design intends never reaches the score, so the analyst_buy / analyst_sell labels I added to SIGNAL_LABELS are currently unreachable in practice.

- **pi-scripts/generate-screener-data.py:175** _( screener )_
  RSI is computed with simple 14-period rolling means of gains and losses rather than Wilder's smoothing, so the displayed number will not match the RSI on TradingView, yfinance or any charting platform. When the gain/loss ratio is NaN or zero it is hard-coded to exactly 50.0, meaning a displayed '50.0' can mean 'no data' rather than 'neutral'.

- **src/components/layout/AppShell.tsx:249** _( shell-and-primitives )_
  MarketStatusPill decides 'open' from a fixed UTC window (`day 1-5 && hour >= 13 && hour < 21`) off the viewer's own clock. It is not DST-aware: in EDT that is 09:00-17:00 ET, so it claims open 30 minutes early and stays open an hour past the 16:00 close; in EST it is 08:00-16:00 ET, opening 90 minutes early. It also ignores US market holidays and half-days. Copy-only mitigation applied (a `title` calling it a rough estimate); the calculation is untouched.

- **src/components/layout/TickerTape.tsx:83** _( shell-and-primitives )_
  The '10Y Yield' entry arrives inside `market_summary.indices` and so goes through the index price path: `formatNumber(idx.price, idx.price >= 1000 ? 0 : 2)` renders it as '4.64' with no percent sign, while the identical value is rendered '4.64%' in archive-client.tsx:179. Its '▲ +0.22%' is also `change_pct` run through formatPercent, so a reader cannot tell whether the yield moved 0.22 percentage points or 0.22% of its own value. Left alone — fixing it means changing how a data field is formatted.


## LOW severity

- **src/app/ticker/ticker-client.tsx:181** _( charts-models-ticker )_
  P/E is rendered straight from the raw float with no rounding, so AAPL displays as 39.55327 — nine characters of false precision under a two-character label. (Sibling issue on line 155: Number(score).toFixed(1) prints '4.0' for a value that is always an integer.) Both are display formatting, so out of scope for a copy change.

- **src/app/charts/charts-client.tsx:70** _( charts-models-ticker )_
  calcEMA and calcRSI/calcATR count their periods in BARS of the selected timeframe, so 'EMA 20', 'RSI (14)' and 'ATR (14)' silently mean 20/14 weeks, months or years on the non-Daily toggles while the labels stay identical. I mitigated this in copy (the subtitle now states the buttons set how much time each bar covers) but the labels themselves still cannot self-describe, and the atr tooltip's 'a normal day's movement' is only exact on the Daily view.

- **src/components/feature/verdict/VerdictBar.tsx:82** _( market-verdict-news )_
  model_features.breadth is a flat 1.00 and its generator is not in this repo (grep finds it only in verdict.json, this component and the zod schema), so what the field measures cannot be verified. I deliberately did NOT attach the glossary's advance/decline definition to it — see the inline comment — because the definition may not be true of this field.

- **src/app/models/models-client.tsx:42** _( market-verdict-news )_
  AccuracyStats and BacktestSummary are both gated with <ProGate feature="walkforward">, so one FeatureKey drives the lock label over three unrelated tiles. I widened the label so it is no longer false, but the correct fix is separate feature keys — that file is outside my area.

- **src/components/feature/options/NopeCard.tsx:16** _( options )_
  The symbol list is hardcoded to SPY and QQQ, so any other symbol present in data.symbols (a z.record, so the schema permits any key) is silently dropped with no indication to the user.

- **src/app/positions/positions-client.tsx:368** _( positions )_
  'Top 10 Avg P&L' hardcodes a leading '+' in the template (`+${acc.overall.top_10_avg_pnl}%`), so a negative value would print as '+-3.1%'. Cosmetic but it can only ever mislead in the bearish direction. Currently masked by the dead-card issue above — it renders for nobody today.

- **pi-scripts/push_dashboard.py:589** _( positions )_
  REPO COPY ONLY — not a live defect, recorded so nobody 'fixes' the site against a stale producer. The checked-in script publishes total_pnl = realized_pnl alone (line 589) while return_pct (lines 555-556) is computed from realized + unrealized, which would make the hero dollar figure and the percentage beside it measure two different things; the same copy computes invested and per-position pnl per-share with no quantity multiplier (line 553). Project memory records that the deployed Pi script was fixed on 2026-07-21 to publish quantity-scaled invested and total_pnl = realized + unrealized, so the live feed does not have these problems. The repo copy is simply behind the Pi. I labelled the UI against the live contract, not this file.

- **src/app/predictions/predictions-client.tsx:389** _( predictions )_
  Mean-Reversion Evolution bar width is Math.min(v.win_rate * 1.2, 100), so the bar length is not proportional to the win rate printed beside it and saturates at 83.3%. Two versions with different win rates above 83.3% draw identical full-width bars.

- **src/app/research/research-client.tsx:1469** _( research )_
  `const beat = r.epsActual >= r.epsEstimate` badges an exactly in-line result as a green 'Beat'. Conventionally, matching the estimate is in-line, not a beat. I worded the new footnote as 'matched or came in above the estimate' so the label is at least described truthfully, but the comparison itself is still wrong by convention.

- **src/app/research/research-client.tsx:1389** _( research )_
  The prediction-market 'Betting closed' badge uses `tone="bear"` (red), which reads as a negative outcome rather than 'this question is over'. A tone change is a display-logic edit, so left alone.

- **src/app/research/research-client.tsx:1064** _( research )_
  When morning_analysis.json meta.market_regime is absent the badge renders a bare '?' with the neutral 'caution' tone. It is not wrong, but it surfaces a missing field as a punctuation mark; an explicit 'Not stated' would be clearer. Left alone as it requires a fallback-value change.

- **pi-scripts/generate-screener-data.py:245** _( screener )_
  Field names are inverted against their computation: `above_52w_high_pct` is computed as (1 - price/high52w)*100, i.e. the percent the price sits BELOW the 52-week high, and `below_52w_low_pct` as (price/low52w - 1)*100, the percent it sits ABOVE the low. The UI happens to use them correctly, but the names invite a sign error in any future consumer.

- **src/app/screener/screener-client.tsx:302** _( screener )_
  Div% renders an em-dash both when divYield is 0 (the generator's stand-in for missing data) and when a company genuinely pays no dividend, so 'no dividend' and 'no data' are indistinguishable. The column is also not sortable, unlike every other numeric column.

- **src/app/screener/screener-client.tsx:584** _( screener )_
  Filter buckets do not line up with the score's own bands, so the UI contradicts itself. RSI 'Strong (55–70)' overlaps extended_rsi, which the score penalises from 65 up; RSI 'Oversold (<30)' is narrower than the score's +2 threshold of 35; and PE 'Premium (25–50)' begins at 25 while premium_pe does not penalise until 30. I left all of these labels alone because each states its own numeric range truthfully — the mismatch is in the thresholds, not the wording.

- **src/app/screener/screener-client.tsx:646** _( screener )_
  Score Min accepts 0.1 steps and defaults to 0 even though compute_score() always returns a whole number clamped to 1–10, and the row pill renders it with .toFixed(1) so an integer 8 reads as the continuous-looking '8.0'. Rows with a null score are treated as 0 at line 86 and so are silently dropped the moment the minimum is raised above 0.

- **src/components/layout/TickerTape.tsx:88** _( shell-and-primitives )_
  FX entries are rendered with `formatNumber(fx.price, 4)` — USD/CAD arrives as 1.41 and is displayed '1.4100', implying two digits of precision the source does not carry. The same mapping deliberately omits `changePct` for FX, so USD/CAD is the only item in the tape with no arrow and no direction, with nothing explaining the asymmetry.
